{% extends "::baseGestion.html.twig" %}


{% block title %}Mettre à jour fiche Categorie {{parent()}}
{% endblock %}
{% block stylesheets %} 
    {{ parent() }}

{% endblock %}


{% block jumbotron %}
    <div class="col-lg-12">
        <h3>Mettre à jour fiche Categorie  : </h3>
        <h4>{{ categorie.nom }}</h4>
        <p class="lead">
        <form role="form" action="{{ path('gestion_core_categories_update', {id:categorie.id }) }}" method="POST" >
        <p>{{form_widget(form)}}</p>
        <input type="submit" class="btn btn-primary" /> 
            <a href="{{ path('gestion_core_categories_delete' , { id : categorie.id} ) }}"  class="btn btn-danger" >supprimer cette fiche</a>
            <a class="btn btn-default" href="{{ path('gestion_core_categories_homepage' ) }}" > 
                Retour Liste </a>
        </form>
        </p>
    </div>
{% endblock %}
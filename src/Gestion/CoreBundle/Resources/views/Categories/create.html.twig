{% extends "::baseGestion.html.twig" %}
{% block title %}Creation Categories {{parent()}}{% endblock %}
{% block stylesheets %} {{ parent() }}{% endblock %}

    {% block jumbotron %}
        <div class="col-lg-12">
            <h1>Creer Categorie</h1>
            <p class="lead">
                <form role="form" action="{{ path('gestion_core_categories_create') }}" method="POST" >
                <p>{{form_widget(form)}}</p>
                <input type="submit" class="btn btn-primary" /> 
                        <a class="btn btn-default"  
                           href="{{ path('gestion_core_categories_homepage' ) }}" > 
                            Retour Liste 
                        </a>               
                </form>
            </p>
        </div>
    {% endblock %}

